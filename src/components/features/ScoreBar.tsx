import React, { useState, useEffect } from "react";
import { colors } from "../../theme";
import type { Candidate } from "../../types";

interface ScoreBarProps {
  candidate: Candidate;
}

// export const ScoreBar: React.FC<ScoreBarProps> = ({ candidate }) => {
//   // ... rest of component
// };

// export default ScoreBar;
